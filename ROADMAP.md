# ROADMAP — remaining work & limitations

Machine-readable twin: `data/roadmap.json` (updated 2026-09-22). Ordered by value. Each item is self-contained enough to start immediately. Statuses cross-checked on 2026-09-19 against data/kalshi/tracker/*.json, data/backtest-results.json and data/contest-results.json.

_Generated by `scripts/gen-roadmap.mjs` — edit `data/roadmap.json`, not this file._

## Status board

| Item | Status | Title |
|---|---|---|
| R1 | DONE 2026-09-19 (live) | Daily automated collection — running on GitHub Actions |
| R2 | DONE 2026-09-19 | 2024 per-state Senate markets captured — backtest 3 → 39 markets |
| R3 | RUNNING — 356 settlements on file (2024 + pre-capture), 0 scoreable 2026 outcomes yet; first scoring event is 2026-11-03 | 2026 forward-collection loop with a live calibration tracker |
| R4 | STARTED 2026-09-19 | Continuous 2026 poll layer — primary-release polls next to the market |
| R5 | DONE 2026-09-19 (standing monitor) | Cross-market consistency checks as a standing monitor |
| R6 | OPEN | 2020 cycle extension (markets did not exist; poll archive does) |
| R7 | PARTIAL — 8 entrants in the 2024 field; the live 2026 field is 12 (R16), and the 2-cycle tournament has not run | Strategy population growth + tournament structure |
| R8 | DONE 2026-09-19 | Documentation hardening |
| R9 | OPEN | Polymarket / PredictIt collectors for the cross-platform monitor |
| R10 | OPEN | Same-day cross-host comparison of the Kalshi API (api.elections vs external-api) |
| R11 | DONE 2026-09-20 (session-8 full re-test batch: SurveyUSA #28000 question text read but report still client-rendered — pending; HarrisX reachable; CourtListener resolved; PEC/Split Ticket #53 stand; WI/NV/CA/MA still blocked; GA + WV Clarity now reachable via the runner's browser profile — VERIFICATION §13c) | Re-verify the hosts that refused the fetch tool, and admit the deferred PA pollster |
| R12 | PARTIAL 2026-09-20 (session 8 added 8 more rows — 23 state rows total: HPU 126 ×2, UH Hobby ×2, Saint Anselm, Stetson CPOR ×2, PPP; CIRCLE youth poll + YESI prior recorded; FHSU/Winthrop/Roanoke verified-issues-only → pending; SurveyUSA MN + UMass Lowell ME + Muhlenberg still pending) | Ingest the newly verified poll sources into the 2026 poll layer as machine-readable rows |
| R13 | DONE 2026-09-20 (standing monitor; first clean live rows for EBO + DDHQ) | Automate the third-party market-rendering cross-check (270toWin, DDHQ Votes) |
| R14 | STARTED 2026-09-20 (first live Metaculus seat rows: MT + NE; State Navigate parser made dual-format after the 2026-09-20 rewording, #64 — first fully-parsed SN row expected from the next live run; scoring waits for Nov 3 canvasses) | Cross-layer scoreboard — Kalshi vs Metaculus vs State Navigate/DDHQ, scored against official canvasses after Nov 3 |
| R15 | RUNNING 2026-09-20 (first committed verdicts landed with run 35490431271, 05:38Z — 30 targets; the session-8 re-test batch is tabled in VERIFICATION §13c) | Host re-test monitor — every previously unreachable URL re-tested from the Actions runner each day, verdicts committed |
| R16 | RUNNING 2026-09-22 (season S1-2026 opened on 3 captured days, 12 entrants, identity holds for all; 9 unranked with published reasons — 1 short of the 3-day minimum market, 8 whose entry windows have not opened in this capture) | Live 2026 paper-trading contest — the forward, mark-to-market season on open Kalshi election markets |

## Items

### R1 · Daily automated collection — running on GitHub Actions — P1 — **DONE 2026-09-19 (live)**
scripts/collect-kalshi.mjs runs in .github/workflows/daily-collection.yml (cron 12:30 UTC on the default branch; also on pushes that touch the collector; opt-out variable COLLECT_DISABLED). First two live runs 2026-09-19 (00:33 and 00:48 UTC): 4,166 series in the Elections|Politics registry, 4,094 open events, 24,367 markets of which 10,709 are traded and still open (plus 207 traded rungs that had already settled). Steady-state footprint ≈ 1 MB/day after the same-day slim-down (irregularity #30). REMAINING: watch the first scheduled (cron) run on main after the PR merges; if the bot commit fails on a push race, the job retries 3× with git pull --rebase. Hardening 2026-09-19: the collector writes the exchange status per market per day, keeps finalized rungs out of the daily rows, refuses to overwrite a universe that halved (shrink guard), and the workflow never publishes a site bundle when pipeline/lint/tests fail (raw data is still committed as the audit record). || Merged-branch forward loop (same workflow since 2026-09-19): scripts/collect-universe.mjs captures the FULL open universe — run #7 (commit 917ee9ef, 04:56 UTC): 14,168 series queried (4,199 politics), 24,150 open markets appended to data/kalshi/forward/tracker.csv, 9,350 settled-2026 markets of which 400 have daily candle bars (seeded once) — 0 errors; the separate universe-collection.yml (12:40 UTC) was retired at merge so there is exactly one daily pass. Session 8 (2026-09-20): irregularity #65 — the 16:22Z scheduled run's commit (d9bbbd7) mixed two fetch states inside universe/latest.json (bySeries 4,124 events vs the file's own 4,087 event list; HOUSECA22 mislabelled as HOUSENY17) after its push-race rebase onto PR #9's merge. Repaired by deterministic replay of the saved events (replayedAt marker), a pre-write self-consistency guard in the collector, and a workflow single-run-snapshot guard (sha256 anchor + recapture-after-rebase) so a pushed commit is always one consistent run.
**Why:** Forward collection is the user's explicit requirement and the 2026 live-cycle evidence base grows daily.

### R2 · 2024 per-state Senate markets captured — backtest 3 → 39 markets — P2 — **DONE 2026-09-19**
scripts/collect-senate-2024.mjs queried all 50 SENATE{ST} series on /historical/markets: 18 states had 2024 markets (36 markets, 1,269 daily bars, every market with an official yes/no result; exactly one YES per state, test-enforced). Backtest now reports core-2024 / senate-2024 / all groups, favourite hit-rate and a de-duplicated pooled calibration curve. REMAINING: the 2024 House-district and governor series were not enumerated (no 2024 governor races of note on Kalshi were checked) — extend the same script to KXGOV*/SENATE*-24 special events if they exist. || Forward-loop capture (scripts/collect-senate-2024-races.mjs → data/kalshi/historical-2024/senate-races.json): same 36 per-state markets WITH candle series → scripts/run-senate-backtest.mjs (data/senate-2024-backtest.json): settlement cross-check vs data/outcomes/senate-2024-official.json 36/36 PASS; T-7 aggregate over 22 scored markets: mean Brier 0.101 (chance 0.25); hold-official-winner at T-7: 11 races, mean price 0.7245 → +0.2755 $/contract edge.
**Why:** A 3-market backtest can detect gross miscalibration but not state-level structure; 39 markets can (indicatively).

### R3 · 2026 forward-collection loop with a live calibration tracker — P3 — **RUNNING — 356 settlements on file (2024 + pre-capture), 0 scoreable 2026 outcomes yet; first scoring event is 2026-11-03**
src/calibration.js scores every settled tracked market at leads 1/3/7/14/30 days and pools a calibration curve; scripts/collect-kalshi.mjs re-checks earlier tickers for settlements every run (GET /markets?tickers=…). As of 2026-09-19: 1 collection day, 0 settlements — the empty state is published as such on the Tracker page. First large batch of U.S. settlements: Nov 3, 2026 (LA mayor, 35 Senate races, governors); primaries/specials before that. REMAINING: nothing to code; let it accumulate, and review the per-lead tables after the first settlements. Update later on 2026-09-19: the first live runs found 207 already-settled rungs nested inside open events (irregularity #37); they are recorded in tracker/settlements.json but the scorer only counts prices dated strictly before a market's settlement day (scoreableMarkets = 0 so far), so no look-ahead can leak into the calibration numbers. Per-run counts are kept in tracker/history.json (site → Tracker → run log). || Forward-loop calibration tracker (scripts/run-calibration.mjs → data/calibration-2026.json): built from the 400 settled-2026 candle-seed markets scored at T-1..T-60 plus a daily headline-series tracker (SENATECONTROL/CONTROL/KXGOV families) that appends every run — this is the 'expected vs actual' layer that grows with R1 collection. First live settlement wave: Nov 3, 2026.
**Why:** Turns the project from a 2024 retrospective into a live, scored forecasting operation ('track expected vs actual').

### R4 · Continuous 2026 poll layer — primary-release polls next to the market — P4 — **STARTED 2026-09-19**
data/polls/poll-layer-2026.json holds 4 generic-ballot series (NBC Jun, Quinnipiac Jul/Sep, Marquette 7-wave trend), 6 state-race polls (CNN/SSRS ME+MI, Emerson TX/MI/IA, ReconMR-Siena TX) and the Cook/Inside Elections ratings, all transcribed from the pollster's own release; src/poll-layer.js compares each to Kalshi (logistic k=4.5, labelled). REMAINING: (a) transcribe Suffolk Iowa (Aug 26), NYT/Siena Jul-1 AK/IA/NC/OH toplines, UNH NH Senate, Marquette Wisconsin; (b) automate a weekly poll-release check (fetch the pollster index pages, diff, open a review list — never auto-admit numbers); (c) once ≥3 polls exist per race, publish a simple poll average with the window and n disclosed. Later the same day: Suffolk Iowa (Aug 20–23, press-release PDF) and the NYT/Siena July-1 AK/IA/NC/OH toplines (Siena release page; NYT n/MoE pages blocked → review flag) were added — 15 entries, 11 state-race rows. Iowa now has three polls (R+5 / R+4 / R+2) around the market's 39.5%. || Parallel continuous-series file data/polls/polls-2026-series.json (9 rows: NBC generic-ballot 2025-03→2026-06, Quinnipiac Jul/Sep 2026, Trump-approval series, Gallup final approval) feeds the site's Forward Loop charts; the June-dated Quinnipiac row was corrected to quinnipiac-2026-07 per irregularity #29 (primary release PDF: Jul 29). Session 9: four primary-release rows added (UNF July FL governor and Nixon/Moody Senate; MSU August LV Senate/governor). LV subset n=779 retained, no guessed MoE/method family; MSU governor comparison withheld until third-candidate wording is verified. Candidate identity gates suppress historical matchup/current nominee mismatches across the poll layer.
**Why:** The 2024 result (polls −4.3pp miss, market closer) is a single-cycle observation; a 2026 replication would make it a finding.

### R5 · Cross-market consistency checks as a standing monitor — P5 — **DONE 2026-09-19 (standing monitor)**
src/consistency.js runs every collect: mutually-exclusive over-sum (high) / under-sum (info, ≥3 markets), crossed books, stale last-vs-book; output data/kalshi/tracker/discrepancy-watch.json (2026-09-19: 46 over / 47 under / 347 stale over 4,094 events). REMAINING: (a) Kalshi-vs-Polymarket divergence alerts need a Polymarket collector (Gamma API) — not built; (b) persist a per-day findings count series so trends are visible.
**Why:** Cheap to compute, and each catch is a published data-quality result.

### R6 · 2020 cycle extension (markets did not exist; poll archive does) — P6 — **OPEN**
Add a 2020 poll-only backtest row (538 2020 averages in the same archive) with the verified official outcome (Biden 306 EV; 81,283,501 votes) to test poll calibration across a wave year vs 2024. No Kalshi markets existed then (exchange launched 2021) — state that explicitly.
**Why:** Multi-cycle poll error distribution is what the user asked for.

### R7 · Strategy population growth + tournament structure — P7 — **PARTIAL — 8 entrants in the 2024 field; the live 2026 field is 12 (R16), and the 2-cycle tournament has not run**
The Leap's ≥3-trading-day rule is implemented (5 of 8 entrants are unranked on core-2024 because of it; 2 are unranked on all-2024). REMAINING: add entrants with distinct theories that only make sense on a wide universe (incumbent-defender on Senate series, seat-count-basis-arb, volatility-scaling), and a 2-cycle cumulative ranking once 2026 settles.
**Why:** The 'competition' is the user's explicit framing; a single 2024 cycle is a pilot, not a tournament.

### R8 · Documentation hardening — P8 — **DONE 2026-09-19**
Dated per-session sections; git-blob SHA-1 (= upstream, checked via the GitHub contents API) and SHA-256 for the three 538 CSVs in data/polls/PROVENANCE.md; iem URL corrected to iem.uiowa.edu/iem/ (irregularity #26). REMAINING: nothing blocking; keep VERIFICATION.md per-session.
**Why:** No-hallucination standard: provenance must be auditable by a third party without network access.

### R9 · Polymarket / PredictIt collectors for the cross-platform monitor — P9 — **OPEN**
Today cross-platform prices are hand-captured snapshots (2026-09-18/19). A daily Polymarket Gamma-API capture of the matching 2026 events (Senate control, key Senate races) would let the consistency monitor flag >5pp cross-platform divergences automatically.
**Why:** Independent venues disagreeing is the cheapest signal that one price is wrong.

### R10 · Same-day cross-host comparison of the Kalshi API (api.elections vs external-api) — P10 — **OPEN**
docs.kalshi.com lists both hosts; irregularity #2 (stale last price) was seen on api.elections.kalshi.com. Run the two-collector cross-check with KALSHI_API_BASE=https://external-api.kalshi.com/trade-api/v2 once and record whether the stale-price behaviour is host-specific (irregularity #31).
**Why:** Determines which host the project should treat as authoritative for last prices.

### R11 · Re-verify the hosts that refused the fetch tool, and admit the deferred PA pollster — P11 — **DONE 2026-09-20 (session-8 full re-test batch: SurveyUSA #28000 question text read but report still client-rendered — pending; HarrisX reachable; CourtListener resolved; PEC/Split Ticket #53 stand; WI/NV/CA/MA still blocked; GA + WV Clarity now reachable via the runner's browser profile — VERIFICATION §13c)**
Re-tested 2026-09-19 (session 6), all eight hosts, results in VERIFICATION.md §11b: surveypoll.com (SurveyUSA) FAILED again (4th), thehillx.com (HarrisX) FAILED again — both stay excluded (#47); courtlistener.com NOW FETCHES and is admitted (master id courtlistener; api.courtlistener.com still unreachable from this fetcher — recorded); election.princeton.edu and split-ticket.org still show no current-cycle content (#53 criteria NOT met, still declined); elections.wi.gov/elections-voting still 'Access denied' (#40), nvsos.gov/sos-elections still 404+CAPTCHA (#41; /sos/elections works), elections.cdn.sos.ca.gov still S3 AccessDenied (#42), sec.state.ma.us division root still 403 while elections-and-voting.htm fetches (#51). Franklin & Marshall admitted after fetching fandmpoll.org's own August 2026 release (master id franklin-marshall-poll; rows fm-2026-08-pa-governor / fm-2026-08-pa-house-preference). REMAINING: keep re-testing SurveyUSA/HarrisX each session (a normal runner may succeed where this fetcher fails — try from the Actions runner). || Session 7 (2026-09-20, VERIFICATION.md §12b): results.surveyusa.com (the archive the corporate site points to) and www.harrisx.com both fetched directly and were admitted (master ids surveyusa, harrisx) — #47 resolved for both brands (surveypoll.com/thehillx.com stay dead and are still probed); CourtListener's v4 API root answered HTTP 200; election.princeton.edu (newest post Nov 5 2024) and split-ticket.org (newest post Oct 20 2025) re-fetched — #53 criteria still not met; WI certified-results path, NV election-information page, CA electionresults.sos.ca.gov, MA electionstats and NH sos.nh.gov all fetched (NH admitted as new-hampshire-sos). REMAINING: the re-tests are now a standing job (R15) — read data/probes/latest.json instead of re-fetching by hand. Session 8 (2026-09-20): the blocked official paths were re-tested from the probe runner (run 35490431271, 05:38Z): WI 'blocked-cloudflare-challenge', NV legacy path 'blocked-akamai-block', CA CDN 'http-403', MA 'blocked-imperva-block' — all still blocked. GA + WV Clarity ENR are now 'reachable' through the runner's browser profile (the plain fetcher still 403s on GA — recorded as a split, #64 era); WV is an improvement over the sessions 3-6 block. SurveyUSA #28000: PollHistory.aspx now fetches with the full question list, but the report page renders empty to the in-session fetcher (probe 'reachable') — no numbers, so no row (pendingSources). HarrisX + CourtListener v4 both resolved in session 7. 2026-09-21 direct re-tests: F&M remains admitted and readable; CourtListener API root and HarrisX readable; SurveyUSA report metadata but no toplines. WI path, CA CDN root and MA directory denied; NV canonical /elections recovered while /sos-elections says Page Not Found. PEC/Split Ticket remain declined under #53. See data/probes/session-review-2026-09-21.json.
**Why:** A registry that only contains what one fetch path can reach silently under-covers exactly the pollsters that matter in the closest 2026 races.

### R12 · Ingest the newly verified poll sources into the 2026 poll layer as machine-readable rows — P12 — **PARTIAL 2026-09-20 (session 8 added 8 more rows — 23 state rows total: HPU 126 ×2, UH Hobby ×2, Saint Anselm, Stetson CPOR ×2, PPP; CIRCLE youth poll + YESI prior recorded; FHSU/Winthrop/Roanoke verified-issues-only → pending; SurveyUSA MN + UMass Lowell ME + Muhlenberg still pending)**
The poll layer still holds 15 hand-transcribed rows. Session 4 added sources whose releases are directly ingestible: the Saint Anselm College Survey Center PDF (June 24-25 2026, n=1,614 NH registered voters, +/-2.4% MoE, weighting stated in the document; its Pappas v Sununu and Pappas v Brown tests map onto SENATENH-26, captured at 0.83/0.84 v 0.17/0.18), CIRCLEs 2026 Youth Poll (n>5,000, fielded Jan 26-Feb 12 2026) and the 2026 Youth Electoral Significance Index (rankings of the Senate and gubernatorial races the youth vote can swing), and the Cooperative Election Study (50,000+ adults per election year, validated vote). Add rows with sourceUrl + verifiedVia + a method-family label (probability panel / RDD-live caller / online post-stratified), then test YESIs race ranking against the markets own volume ranking as an independent prioritisation check. || Session 6: ingested the first machine-readable rows from the four session-5 pollsters with #49 methodFamily labels — UMass Amherst Aug 21–26 (House generic D 42/R 34, Senate generic D 40/R 38; online-nonprobability-matched), Fox News Sept 11–14 (generic D 51/R 44; voter-file-hybrid), NPI AZPOP Aug 10–13 (AZ governor LV Hobbs 48/Biggs 35 → GOVPARTYAZ-26; online-optin-weighted), F&M Aug 17–23 (PA governor Shapiro 50/Garrity 25 → GOVPARTYPA-26; PA House preference D 48/R 36; voter-file-mail-recruit). Muhlenberg has NO 2026 horse-race release on its own site (poll library checked) — recorded under pendingSources, no row fabricated. REMAINING: Saint Anselm / CIRCLE / CES rows from session 4; add methodFamily to the pre-session-6 rows where the release states the design. || Session 7: elon-2026-09-nc-senate (Cooper 49 / Whatley 38 LV, Aug 21–31, n=800, MoE ±5.6 → SENATENC-26; online-nonprobability-matched) and harrisx-2026-08-generic (Harvard CAPS/HarrisX Aug 28–30, n=2,100 RV, D 51 / R 49 two-way, no MoE published → moe null; online-optin-weighted). Also hpu-2026-04-nc-senate (HPU Poll 120, fielded by YouGov Mar 26–Apr 6, Cooper 50 / Whatley 42 among 703 LV, credibility interval ±4.3 → SENATENC-26; online-nonprobability-matched). Declined with reasons in pendingSources: UMass Lowell's Maine test uses Platner, not the priced nominee Troy Jackson (#60); SurveyUSA #28000 Minnesota is a client-rendered report (probe target added). REMAINING: Saint Anselm / CIRCLE / CES rows, methodFamily on the 15 legacy rows, HPU Poll 126 and Winthrop once their release pages are read. Session 8 (2026-09-20) ingested: hpu-2026-08-nc-senate (Cooper 50/Whatley 45, LV 660, CI ±5.2), hpu-2026-08-nc-house-generic (47/47; kalshiDemTicker NULL BY DESIGN — marketBasis explains the KXHOUSEWINSTATE-NCD seat-count distinction, and the site now renders that note instead of a bare 'null'), uh-hobby-2026-01-tx-governor (Abbott 49/Hinojosa 42, n 1,502 ±2.53), uh-hobby-2026-01-tx-senate (median D 43 / R 45.5 over six candidate-matched scenarios — the reduction is stated), saint-anselm-2026-06-nh-senate (Pappas 47/Sununu 41, RV 1,614 ±2.4, new family random-cellphone-rv-panel, published marketGap vs SENATENH-26-D ≈ 0.83), stetson-2026-04-fl-governor (Donalds 47/Jolly 40) + stetson-2026-04-fl-senate (Moody 49/Vindman 42 → SENATEFLS-26, the short-term seat), ppp-2026-07-nc-senate (Cooper 48/Whatley 44, n 759 ±3.6; methodFamily NULL by #49 discipline). CIRCLE's 2026 Youth Poll (n 5,549, 57/43 partisan, D/R null — subpopulation) is an aggregator reading; YESI 2026 top-10 lists are independentPriors. FHSU Kansas Speaks (policy-only, Figures 14-15 recognition/evaluation), the Winthrop 2026-july national (all 26 tables issues-only) and Roanoke (Feb 2026 issues-only) are verified and deliberately not ingested (pendingSources). The earlier four-source batch (UMass Amherst, Fox, F&M, NPI) already has poll-layer entries. Muhlenberg remains issues-only at re-test, SurveyUSA toplines unavailable; do not manufacture completion. Extend automated primary-release ingestion rather than treating registry admissions as polls.
**Why:** Verified sources only become evidence once they are recorded as dated forecasts the scorer can judge after settlement — and YESI/CES give pre-election priors that are not market-derived.

### R13 · Automate the third-party market-rendering cross-check (270toWin, DDHQ Votes) — P13 — **DONE 2026-09-20 (standing monitor; first clean live rows for EBO + DDHQ)**
scripts/crosscheck-renderings.mjs runs daily in daily-collection.yml (continue-on-error) and writes data/kalshi/tracker/rendering-crosscheck.json: Election Betting Odds' Kalshi row for Senate Control 2026 (58.4–59.4% on 2026-09-19) is compared at the midpoint with captured CONTROLS-2026-D (0.59/0.60) and flagged only beyond 3 points; DDHQ Votes' House/Senate odds (70% / 52%) are stored as a context layer; 270toWin's Kalshi panel (2028 presidency, '57%/41% as of Sep. 19, 2026 at 20:29 UTC') is recorded and compared only when a matching captured market exists. Parse failures are stored verbatim (extract:'failed' + sample). Offline tests in test/crosslayer.test.mjs use transcribed fixtures. REMAINING: prove the first live run on the Actions runner and add Polymarket/PredictIt collectors (R9) so EBO's other rows can be checked too. || First live run 2026-09-20 (#58): all three extract:'failed' on raw-HTML entities; parsers fixed against the recorded samples; next scheduled run is the proof. || Second live run 2026-09-20 (run 35485210251): EBO parsed (Kalshi Senate-D 0.584–0.594 vs captured mid 0.595, diff −0.006, not flagged), DDHQ parsed (House D 70 / Senate D 52), 270toWin extract:'failed' because the raw homepage HTML lacks the panel's numbers (client-side) — the monitor now renders 270toWin in headless Chrome when the plain HTML does not parse and labels the row fetchMethod (#62). Session 9 hardening: reject incomplete/crossed/nonfinite books; do not compare captures more than 24 hours apart; compare EBO midpoint and 270toWin last trade. Fixed dashboard dem/rep keys. Lightweight workflow now rebuilds scores/site and runs full tests before committing, with no forced conflict resolution.
**Why:** Two independent consumers of the same exchange data agreeing is cheap corroboration that our capture is right; disagreeing is the cheapest signal that one of the three is stale.

### R14 · Cross-layer scoreboard — Kalshi vs Metaculus vs State Navigate/DDHQ, scored against official canvasses after Nov 3 — P14 — **STARTED 2026-09-20 (first live Metaculus seat rows: MT + NE; State Navigate parser made dual-format after the 2026-09-20 rewording, #64 — first fully-parsed SN row expected from the next live run; scoring waits for Nov 3 canvasses)**
Built this session: src/crosslayer.js (Brier + log-loss per layer, midpoint for bid/ask layers, look-ahead guard, refuses outcomes without an official source url); data/crosslayer/snapshots.json (seed rows 2026-09-19: Senate — Kalshi 0.59/0.60, Metaculus 0.517, DDHQ 0.52, EBO-Kalshi 0.584/0.594; House — Kalshi 0.89/0.90, Metaculus 0.888, DDHQ 0.70); data/crosslayer/outcomes.json (EMPTY by design, lists the official canvass sources to use); scripts/collect-metaculus.mjs (hub HTML parser, optional METACULUS_API_TOKEN, appends one snapshot per question per day and pairs same-day Kalshi captures); scripts/collect-statenavigate.mjs (free forecast pages + daily API-host probe; #55). Both collectors are wired into daily-collection.yml as continue-on-error steps. REMAINING (P0 after Nov 3): fill outcomes.json from the state certifications (per-state SoS master entries, NASS), run scoreSnapshots, and publish the per-layer table; extend the questions to the 35 Senate races (Metaculus race pages) so the '18 of 35 lean D' claim can be scored seat by seat. || First live run 2026-09-20 (#58): Metaculus collector died before writing (fixed: always writes a row); State Navigate pages are client-rendered — needs a headless-browser step (open). || Session 7: QUESTION_PAGES pairs Metaculus per-seat questions with Kalshi SENATE{ST}-26-D by event title (Kentucky is SENATELA-26 on the exchange, #59); first live seat rows 2026-09-20 — Montana (Metaculus D 0.01 vs Kalshi 0.002/0.013) and Nebraska (0.001 vs 0/0.001) — plus the Senate-plurality question (D 50.6 / R 49.3, 459 forecasters). Cloudflare let headless Chrome through on 3 of 7 pages (#62): renderDom now keeps one profile per run, retries, and the collector makes a second pass. REMAINING: watch the next runs for the hub row; extend QUESTION_PAGES as Metaculus opens more seat questions; after Nov 3 fill outcomes.json from the per-state certifications. || Third live run (#63): rendered hub misread (Senate = House 88.8) and snapshotted; overlapping runs overwrote rows via rebase -X theirs. Fixed: nearest-chamber-word parser + quadrant cross-check gate, wrong row deleted, MT/NE rows restored from git, shared concurrency group, one daily row per run. Governor questions parsed (HI/NM/OR/MI) → first governor seat rows. Session 8 (2026-09-20): State Navigate reworded the national + chamber pages between 09-19 and 09-20, so the first live row parsed 'failed' (national headline + all 34 chambers; only the D/R pickup counts matched) — irregularity #64. parseNational/parseChamber are now dual-format (both layouts are fixtures asserted in test/crosslayer.test.mjs); the failed row was NOT backfilled (provenance rule). CIRCLE YESI 2026 ↔ Kalshi-volume cross-check written to data/polls/yesi-vs-kalshi-volume.json (senate 6/10, governor 6/10, house 3 states — basis mismatch documented). Metaculus: the 2026-09-20T03:16Z pre-gate row remains the #63 audit trail; scoring stays pending by design until the Nov 3 official canvasses land in data/crosslayer/outcomes.json. Session 9: npm run crosslayer writes certification-gated scores; approved authority origins, dated evidence, parsed timestamps, invalid probability rejection, exact Brier, and one paired observation per question. All 2026 outcomes remain empty. State Navigate API session fetch still HTTP 500 (local Node ECONNRESET); free pages readable, no verified API endpoint or paid tier used. Automatic jurisdiction-specific canvass ingestion and chamber-control derivation are NOT implemented; see backtest/crosslayer-outcomes.md.
**Why:** The user's P0: settle the Metaculus-vs-Kalshi Senate gap with official results, not opinion; the scorer exists now so the first canvass can be scored the day it is certified.

### R15 · Host re-test monitor — every previously unreachable URL re-tested from the Actions runner each day, verdicts committed — P15 — **RUNNING 2026-09-20 (first committed verdicts landed with run 35490431271, 05:38Z — 30 targets; the session-8 re-test batch is tabled in VERIFICATION §13c)**
scripts/probe-hosts.mjs reads data/probes/targets.json (30 targets: SurveyUSA hosts + the #28000 report page, HarrisX/The Hill, CourtListener API, PEC, Split Ticket, WI/NV/CA/MA/NH official paths, electionresults.sos.ca.gov, Georgia + West Virginia Clarity ENR, NYT/Siena toplines, healthyelections.org, Selzer, State Navigate API host + national page, Metaculus hub + Senate states, Muhlenberg, F&M, 270toWin, cnalysis redirect, HPU Poll 120, Winthrop) and writes data/probes/latest.json + history.json with status, challenge type, title, a text sample and — for render:true targets — the headless-Chrome DOM verdict; raw bodies go to the git-ignored data/probes/raw/ (uploaded as a workflow artifact). Runs in collector-probe.yml (push fast lane) and daily-collection.yml (cron). First live run 2026-09-20 crashed with ENOENT on the raw directory before writing anything, behind a green continue-on-error step (#61) — fixed (directory created up front; per-target try/catch → verdict 'error'). REMAINING: read the first committed latest.json; promote any 'reachable' verdict for an excluded host into a re-test addendum on its master entry; add targets as new exclusions appear. Session 8 (2026-09-20): the first committed latest.json (run 35490431271, 05:38Z) covers all 30 targets — highlights: ga-clarity 'reachable' (browser profile) vs the plain fetcher's 403 (split recorded), wv-clarity 'reachable' (improved from the sessions 3-6 block), harrisx + courtlistener-api 'reachable', surveyusa-surveypoll 'error' (dead SSL host), thehillx 'error' (dead), selzerco 'reachable' (empty shell — #48 stands), WI/NV/CA/MA still blocked (cloudflare/akamai/403/imperva), metaculus hub 'reachable-rendered-only', statenavigate national 'reachable'. Standing rule unchanged: judge continue-on-error steps by the committed files. (The first committed latest.json also exposed #65: its run's universe commit was a two-fetch-state splice — repaired in session 8, see R1/#65.)
**Why:** The re-test list was re-fetched by hand every session; a job that produces dated verdicts turns 'still blocked' into evidence and notices recoveries (like CourtListener's) the day they happen.

### R16 · Live 2026 paper-trading contest — the forward, mark-to-market season on open Kalshi election markets — P16 — **RUNNING 2026-09-22 (season S1-2026 opened on 3 captured days, 12 entrants, identity holds for all; 9 unranked with published reasons — 1 short of the 3-day minimum market, 8 whose entry windows have not opened in this capture)**
The 2024 contest could only settle markets whose results were already known. R16 runs the same field forward against still-open markets, which is what the brief actually asks for: a paper-trading simulation with unique usernames and unique strategies whose PnL is tracked on the open political/election markets. Built this session: src/contest/forward-universe.js (builds the contest universe offline from the committed captures — US-election-tagged series, present in that day's panel, two-sided book, close after the day; every exclusion carries a reason code and the per-day reasons are tallied in universe.json, including `closes-after-season-window` for the 2028-cycle rows that are open today but settle years from now (irregularity #78)), src/contest/forward-engine.js (replays each captured day, taker-only fills at the captured book — BUY YES pays yes_ask, BUY NO pays 1 − yes_bid — with a participation cap of 10% of the day's volume and 10% of open interest, a pro-rata daily deployment ceiling of 20% of start-of-day equity that is asserted order-independent, per-entry fee accounting that keeps netEquity = 100,000 + realized + unrealized exact, and settlement only from a captured official result), src/contest/strategies-forward.js (12 entrants: the 7 transferable 2024 strategies imported unchanged — the test asserts object identity of their decide() functions — plus 5 new 2026 theses: poll-anchor-26, ratings-ratchet, crosslayer-arb, longshot-fader, combo-coherence; and the identity gate that refuses to trade a poll whose candidate is not the candidate the Kalshi leg resolves on), scripts/run-forward-contest.mjs (writes season.json, universe.json, leaderboard.json/csv, equity.csv, fills.csv, attribution.json and signals-ledger.csv; it FAILS LOUDLY if the captured universe carries no market sub-titles, because that would silently disarm the poll identity gate — a failure mode this session hit and then pinned with a test), test/forward-contest.test.mjs (35 tests), scripts/build-market-list.mjs (the canonical open-market list, 24,102 rows, with the reconciliation gate 10,981 + 13,120 + 200 = 24,301 and a published reason for every row), and the site section 'Live 2026 Contest' (/contest2026/). REMAINING: (a) persist the computed candidate-mismatch flag onto the poll rows (#77) so the site, the contest and any direct reader cannot disagree; (b) verify each unranked entrant the day its entry window opens rather than assuming it will fire; (c) convert from mark-to-market to realised when the 2026-11-03 results are certified; (d) model maker fills and order-book depth, which the captured top-of-book data does not support today; (e) decide whether season.json's fill log rotates monthly; (f) reconcile the crosslayer-arb fills against the captured cross-layer snapshot set, which is still unexplained.
**Why:** A contest that can only replay settled history cannot test a forecasting thesis; the brief asks for PnL tracked on today's open markets, and only a forward season produces an out-of-sample verdict.

## Current limitations

- One collection day so far (2026-09-19). The live calibration scorer is empty by construction until markets that were priced before settlement settle; 207 settlements already on file are pre-tracker settlements found nested in open events and are deliberately not scored (a post-settlement price is not a forecast). First large batch: Nov 3 2026 (Los Angeles mayor, 35 Senate races, governors). Both collection loops (compact tracker + FULL universe) now run inside the single daily-collection.yml pass at 12:30 UTC.
- 2024 backtest universe = 39 settled markets from one cycle (3 core + 36 per-state Senate, 18 states). Most Senate markets opened in October 2024, so T-30/T-60 lead times cover few markets; the calibration curve is indicative, not conclusive, and the 2024 Senate map was a Republican year — group results are not exchangeable across cycles.
- The daily tracker stores traded markets only (volume > 0 or open interest > 0). Never-traded strike-ladder markets are counted in the per-day meta file but their (empty) quotes are not kept; a market that trades for the first time enters the tracker on that day.
- Poll-implied probabilities use a logistic mapping (k = 4.5) chosen in the 2024 poll backtest; it is a labelled modelling assumption, not a pollster claim. Rating 'bands' used to flag market-vs-rating gaps are a project heuristic (documented in poll-layer-2026.json).
- The 2026 poll layer is hand-transcribed from primary releases (15 entries; 4 NYT/Siena rows carry a review flag because n / MoE / field dates sit on nytimes.com pages that return 403 to the fetcher). It is not a poll average and it is not complete — e.g. Suffolk Iowa and the NYT/Siena July state polls are known but not yet transcribed. Nothing is auto-ingested.
- Exit-poll data: none used. The 2026 product (The Voter Poll by SSRS, successor to the Edison/NEP exit poll and AP VoteCast) exists but will produce data only on Nov 3, 2026.
- NO-side prices in captured 2024 candles are derived reciprocals (1 − yesClose); the raw API only publishes the YES-side series. Contest NO fills are labelled as derived.
- Poll data for 2024 ends 2024-09-12 (538 archive cutoff); the poll-vs-market comparison after that date reuses the anchor with its age disclosed.
- Two collectors run minutes apart, so the cross-check tolerates 2¢ moves; it is an agreement report, not a proof that either capture is complete. Some official results hosts block automated fetchers (irregularity #35).
- GitHub Pages serves the committed site bundle (src/data/site-data.js, ~1.2 MB); it updates only when the daily workflow commits (default branch) or a PR merges. The hand-captured Kalshi hub snapshot of 2026-09-18 is kept for cross-platform checks and is dated on the page.
- Kalshi's events feed (status=open, nested markets) returns settled (finalized) rungs inside still-open multi-market events. The collector now records their status and results and excludes them from the tradable/tracked set, but the API semantics (why finalized markets stay nested for weeks) are undocumented in the pages fetched — treat 'open event' ≠ 'every rung tradable'.
- Source registry: 125 entries verified across five sessions (six batches; 20 added in session 5 on 2026-09-19), but verification is point-in-time. Sessions 4 and 5 re-fetched their 40 new hosts on 2026-09-19 only, and the daily workflow does not re-fetch source pages, so a URL that changes later is not caught until a session re-verifies it (LIMITATIONS #18).
- Categories on the master list are an organisational label added 2026-09-19 so the site can group 125 entries; they come from a fixed vocabulary reviewed entry-by-entry (VERIFICATION.md §9b/§10) and carry no claim about a source tier or reliability ranking.
- Third-party renderings of Kalshi prices (270toWin, DDHQ Votes) are corroboration surfaces only: they are last-trade based, may not sum to 100%, and can mix capture dates on one page (irregularity #46). Every price used in models, backtests and the contest comes from captured yes_bid/yes_ask.
- Fetch-tool reach is a coverage limit, not a data problem: surveypoll.com (SurveyUSA) and thehillx.com (HarrisX) still refused the proxied fetcher on the session-6 re-test (2026-09-19); courtlistener.com recovered and was admitted. Official paths WI /elections-voting, NV /sos-elections, CA elections.cdn.sos.ca.gov and the MA division root are still blocked (deep/alternate pages fetch). Those sources are excluded or annotated rather than reconstructed from memory (irregularities #40-#42, #45, #47, #51).
- Cross-layer scoreboard (R14) is pending by construction: data/crosslayer/outcomes.json is empty until official canvasses are certified after Nov 3; the Senate spread (Kalshi 59-60 cents vs Metaculus 51.7% vs DDHQ 52%, all 2026-09-19) is recorded (#57), not adjudicated.
- Metaculus is parsed from the server-rendered hub because api2 requires authentication (#54). State Navigate's forecast pages are CLIENT-rendered (first live run 2026-09-20: 0 of 35 pages parsed; API host answers an empty shell, #55/#58), so its numbers are not yet collected automatically — a headless-browser step is required. The R13 rendering monitor's entity-decoding bugs found on the same run are fixed; its first clean run is still pending.
- Metaculus behind Cloudflare is best-effort from the runner: plain fetches are 403 on both profiles and headless Chrome passed the check on 3 of 7 pages in the first live run (#62). renderDom now keeps one profile per run and retries, and the collector makes a second pass — but whether the interstitial keeps clearing is outside this project's control; every row carries fetchMethod/fetchLog so a reader can tell a rendered number from a missing one.
- State Navigate's numbers have still not been collected automatically: the second live run fetched all 35 pages but a nav keyword ('close seats') satisfied the render trigger, so Chrome was never used (#62). The trigger is now the parser itself; the next committed forecast-daily.json is the proof. Until then the Cross-layer section keeps the hand-verified 2026-09-19 figures, labelled as such.
- The host re-test monitor (R15) has no committed verdicts yet: its first live run crashed before writing latest.json and the continue-on-error step stayed green (#61). Fixed and verified locally with a stand-in Chrome; the first real latest.json arrives with the next push/cron run. Rule from #58/#61: judge a continue-on-error step by the file it should have committed, never by its badge.
- Poll rows added this session are three (Elon and HPU NC Senate, HarrisX generic). HarrisX publishes no margin of error for its opt-in panel (moe null, not estimated); UMass Lowell's Maine test names a non-nominee and SurveyUSA's Minnesota report is client-rendered — both are pendingSources with reasons, not race rows (#60, #47).
- Kalshi tickers do not always encode the state (SENATELA-26 is Kentucky, #59). The Metaculus pairing now checks the event title, but every other place a ticker is built from a state code (contest strategies, poll-layer kalshiDemTicker) still assumes SENATE{ST}-26 — audit those paths before any per-seat scoring after Nov 3.
- Actions logs and artifacts cannot be read from the sandbox (only committed files); a collector that fails without committing leaves no trace except an absent file. Every collector now writes a row even on failure, and the probe writes a verdict per target — but a crash before the first write would still be invisible until someone looks for the file.
- The Metaculus hub parser has now been wrong once in production (#63: Senate read as the House number from the rendered DOM). The quadrant cross-check gate means a repeat cannot reach the scoreboard, but the rendered DOM itself is not committed (raw bodies are 7-day artifacts unreachable from the sandbox), so a new layout change is diagnosed from the row's sample and flags, not from the page.
- 2026-09-21: the new cross-layer certification gate validates metadata and approved origins, not the semantic truth of a quoted document. Post-election automatic canvass parsers and chamber-control derivation remain required. Current scores are pending, not zero-loss predictions.
- Follow-up on 2026-09-21 added 14 county election-authority pages plus the League of Women Voters homepage and Verified Voting. Those pages are not certified canvasses. Baltimore City, Denver, Multnomah, and Salt Lake were already admitted and were not duplicated. Ten other candidate URLs were shells, a wrong path, a 404, or a failed fetch (irregularity #71). Vote.org and Rock the Vote were recategorized out of government (irregularity #70).
- The live 2026 contest is three captured days old and every published 2026 number is a mark-to-market snapshot, not a result: open positions are marked to the last captured bid/ask mid and no 2026 market has settled. The page labels this on every entrant row (season.json model.ranking, leaderboard.rankedAsOfNote).
- Source verification in session 12 used the page-fetch channel, not the collectors' own host path: this sandbox cannot open TLS to the Kalshi API (irregularity #22 re-confirmed), so the two market-rule captures and the fee-schedule re-read came through the fetch tool while the collectors keep running from the Actions host. The captures agree with the committed 2026-09-21 panel to within the overnight move, which is corroboration, not proof of equivalence.
- The 2026 control markets resolve on the party of the President pro tempore of the Senate / Speaker of the House on 2027-02-01, with an early media-call determination permitted (captured rule text, irregularity #72). 'Control' is therefore not the same event as a 51-seat majority, and no model in this repository may assume the majority reading — it was queued as an assumption and is now explicitly withdrawn.
- Contest edition parameters are not stable: The Leap's own rules set the minimum trading days per edition (3 in the February 2026 Eurex rules, 5 in the later editions observed) and the balance differs by edition too ($100,000 in the May 2026 crypto and July 2026 editions, $250,000 in September 2026). The live season copies the $100,000/3-day configuration and states which edition it copies (irregularity #74); a future edition would need the constants re-read, not assumed.
- The Kalshi fee schedule's maker default multiplier is 0, while src/fees.js documents a single default of 1 (irregularity #76). No published number is affected — every one of the 1,413 US-election series in the captured registry carries multiplier 1, and the contest is taker-only — but the maker default must be corrected before any maker-fee feature exists.
- The live contest's fills are taker-only at the captured top of book and capped by participation, so its PnL is a lower bound on execution friction rather than a simulation of it: no order-book depth is captured, so walking the book, partial fills within the book and queue position are all unmodelled.
- Five transferred entrants have placed no trade at all in the first three days because their 2024 entry rules need 5-14 days of history or sit inside a window that has not opened (momentum-mule, fader-flipper, yield-yak, shock-surfer, breakout-bandit), and combo-coherence found no basket that cleared its fee-aware threshold. They are published as unranked with reasons; their theses remain untested rather than disproven.

See also `LIMITATIONS.md` (narrative) and `NEXT_SESSION.md` (start-here checklist).
